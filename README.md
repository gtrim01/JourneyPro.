# JourneyPro

Trip planning for Australian caravan & 4WD tourers — vehicle-aware fuel costs,
fill plans with real roadhouses, stop guides, live weather, and whole-trip budgets.

Prototype v0.7. Built with React + Vite. Deployed on Vercel.

Local development (optional): `npm install` then `npm run dev`.
